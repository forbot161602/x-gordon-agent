# Standing instructions

Below, the user speaks in the first person; each entry holds until they withdraw it.

## Delegation

I am requesting the Agent tool for the procedures this project runs. Where a command, a skill, a memory, or any other instruction the agent follows here sets out a step that dispatches subagents, that step is **requested by me**: it MUST be carried out as written rather than weighed as an option, and a procedure that splits its work across subagents is not run in a single context instead. The invocation already grants it, per `agent-autonomy`, and the grant carries this request down, so a subagent whose own steps dispatch further holds it too.
